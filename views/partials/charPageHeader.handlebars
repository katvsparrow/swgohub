<header class="grad">

  <div class="header-image-container">
  	<a href="/characters/{{name}}">
		<img class="{{alignment}} header-image character-page-character" data-name="{{name}}" data-alignment="{{alignment}}" data-rank="{{rank}}" data-faction="{{#each faction}} {{this}} {{/each}}" src="{{pic}}" />
	</a>
  </div>

  <h1 class="character-page-title">{{name}}</h1>

</header>